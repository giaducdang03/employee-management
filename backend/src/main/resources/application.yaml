spring:
  application:
    name: employee_demo
  datasource:
    url: jdbc:postgresql://localhost:5432/employee_fa
    username: postgres
    password: Abc@12345
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect